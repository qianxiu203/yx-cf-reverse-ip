# Cloudflare Pages / Wrangler 配置文件
# 本地开发使用: npx wrangler pages dev public

name = "cf-reverse-ip"
pages_build_output_dir = "public"
compatibility_date = "2025-01-01"

# Pages Functions 配置
# 函数默认存放在 /functions 目录
